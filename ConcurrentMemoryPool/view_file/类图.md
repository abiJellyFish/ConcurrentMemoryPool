# 并发内存池 - 类图

```mermaid
classDiagram
    class ThreadCache {
        -FreeList _freeLists[FREE_LIST_NUM]
        +Allocate(size_t size) void*
        +Deallocate(void* ptr, size_t size) void
        +FetchFromCentralCache(size_t index, size_t size) size_t
        +ListTooLong(FreeList& list, size_t size) void
    }
    
    class CentralCache {
        -SpanList _spanLists[FREE_LIST_NUM]
        +FetchRangeObj(void*& start, void*& end, size_t batchNum, size_t size) size_t
        +GetOneSpan(SpanList& list, size_t size) Span*
        +ReleaseListToSpans(void* start, size_t size) void
        +GetInstance() CentralCache*
        -CentralCache()
    }
    
    class PageCache {
        -SpanList _spanLists[PAGE_NUM]
        -TCMalloc_PageMap3 _idSpanMap
        -ObjectPool~Span~ _spanPool
        -mutex _pageMtx
        +NewSpan(size_t k) Span*
        +MapObjectToSpan(void* obj) Span*
        +ReleaseSpanToPageCache(Span* span) void
        +MapSpanToPageID(Span* span) PageID
        +GetInstance() PageCache*
        -PageCache()
    }
    
    class TCMalloc_PageMap3 {
        +set(Number key, void* value) void
        +get(Number key) void*
        +erase(Number key) void
        +Ensure(Number start, size_t n) bool
    }
    
    class ObjectPool~T~ {
        +New() T*
        +Delete(T* obj) void
        -mutex _poolMtx
    }
    
    class ConcurrentAllocator~T~ {
        +allocate(size_type n) pointer
        +deallocate(pointer p, size_type n) void
        +construct(U* p, Args&&... args) void
        +destroy(U* p) void
        +max_size() size_type
    }
    
    class Span {
        +size_t _objSize
        +size_t _n
        +PageID _pageID
        +void* _freeList
        +Span* _next
        +Span* _prev
        +bool _isUse
        +size_t use_count
    }
    
    class FreeList {
        +void Push(void* obj)
        +void* Pop()
        +bool Empty()
        +size_t Size()
        +PushRange(void* start, void* end, size_t size)
        +PopRange(void*& start, void*& end, size_t size)
    }
    
    class SpanList {
        +PushFront(Span* span)
        +PopFront() Span*
        +Insert(Span* pos, Span* ptr)
        +Erase(Span* pos)
        +Begin() Span*
        +End() Span*
        +Empty() bool
        -mutex _mtx
    }
    
    class SizeClass {
        +RoundUp(size_t size) size_t
        +Index(size_t size) size_t
        +NumMoveSize(size_t size) size_t
        +NumMovePage(size_t size) size_t
    }
    
    ThreadCache --> CentralCache : uses
    CentralCache --> PageCache : uses
    PageCache --> TCMalloc_PageMap3 : uses
    PageCache --> ObjectPool~Span~ : uses
    ConcurrentAllocator~T~ --> ConcurrentAlloc : uses
    CentralCache --> Span : manages
    PageCache --> Span : manages
    ThreadCache --> FreeList : contains
    CentralCache --> SpanList : contains
    PageCache --> SpanList : contains
    SpanList --> Span : contains
```

## 类关系说明

| 关系 | 类A | 类B | 说明 |
|------|-----|-----|------|
| uses | ThreadCache | CentralCache | 线程缓存向中央缓存申请内存 |
| uses | CentralCache | PageCache | 中央缓存向页面缓存申请Span |
| uses | PageCache | TCMalloc_PageMap3 | 页面缓存使用基数树映射页号 |
| uses | PageCache | ObjectPool~Span~ | 页面缓存使用对象池管理Span |
| uses | ConcurrentAllocator | ConcurrentAlloc | 标准分配器调用并发内存池接口 |
| manages | CentralCache | Span | 中央缓存管理Span链表 |
| manages | PageCache | Span | 页面缓存管理Span链表 |
| contains | ThreadCache | FreeList | 线程缓存包含多个空闲链表 |
| contains | CentralCache | SpanList | 中央缓存包含多个Span链表 |
| contains | PageCache | SpanList | 页面缓存包含多个Span链表 |
| contains | SpanList | Span | Span链表包含Span节点 |

## 核心类职责

| 类 | 职责 |
|----|------|
| ThreadCache | 线程本地缓存，无锁分配，减少锁竞争 |
| CentralCache | 全局缓存，桶级锁，批量分配/释放 |
| PageCache | 页面级缓存，全局锁，向系统申请内存 |
| TCMalloc_PageMap3 | 三层基数树，O(1)页号到Span映射 |
| ObjectPool~Span~ | Span对象池，高效创建/销毁Span |
| ConcurrentAllocator | C++标准分配器接口，适配STL容器 |
| Span | 内存页管理单元 |
| FreeList | 空闲内存块链表 |
| SpanList | Span双向链表 |
| SizeClass | 内存大小对齐和哈希桶索引计算 |
